# Environment variables — copy this to .env and fill in your values
# Never commit .env to version control!

# Optional: OpenAI API key (only needed if using OpenAI embeddings/LLM)
OPENAI_API_KEY=your-api-key-here
